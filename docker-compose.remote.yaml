services:
  manager:
    image: ghcr.io/lowkruc/warp-proxy-manager:v1.1.0
    ports:
      - "8080:8080"
      - "1080:1080"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./config.yaml:/app/config.yaml
    restart: unless-stopped
